<%= form_for @playlist do |f| %>
	<%= render 'shared/error_messages', object: f.object %>

	<%= f.text_field :title, placeholder: "New Playlist Title" %>
	<%= f.submit "Create Playlist", class: "create-playlist" %>
<% end %>
